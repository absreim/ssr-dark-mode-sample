# ssr-dark-mode-sample

This project is meant to serve as an educational example of implementing dark mode with Bootstrap and Next.js It was
created with Create Next App.

## Getting Started

To run the project, first, install NPM dependencies if you have not already done so.

```bash
npm install
```

Then, run the `dev` NPM script:

```bash
npm run dev
```